<div class="col-md-3 col-sm-6 hero-feature">
    <div class="thumbnail">
        <img src="{{asset('img/products/'~product.imageProduct.url~'.jpg')}}" alt="Nom du produit">
        <div class="caption">
            <h3>{{ product.name }}</h3>
            <p>{{ product.description }}</p>
            <p>
                <span class="badge">120,00 €</span>
                <a href="store/product/{{ product.id }}/details/{{ product.slug }}" class="btn btn-default">Voir la fiche</a>
            </p>
        </div>
    </div>
</div>